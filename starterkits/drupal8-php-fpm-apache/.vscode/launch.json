{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for XDebug for Vega Application.",
      "type": "php",
      "request": "launch",
      "port": 9001,
      "log": true,
      "pathMappings": {
        "/app": "${workspaceFolder}"
      }
    }
  ]
}
